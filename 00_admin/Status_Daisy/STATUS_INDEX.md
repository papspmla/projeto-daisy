PROJETO DAISY — STATUS INDEX

All operational status records are stored in this directory.

Each development session generates one independent status file.

Example:

STATUS_DAISY_v1_31.md

This prevents the operational status file from growing indefinitely
and keeps session records modular.

Snapshots remain stored in:

../Snapshots
